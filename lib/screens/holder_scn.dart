import 'package:flutter/material.dart';

class HolderScn extends StatelessWidget {
  const HolderScn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Screen Holder"),
      ),
    );
  }
}
